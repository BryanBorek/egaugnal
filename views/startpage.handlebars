<aside class="login-glass">
    <h3 class="mt-4">Egaugnal</h3>
    <div class="col">
        <button id="logout" class="btn btn-light mt-2 mb-2" type="submit">Logout</button>
    </div>
    <div>
        <button class="btn btn-primary" type="submit">High Scores</button>
    </div>
</aside>
<div class="h-100 mt-5">
    <div class="login-card">
        <div class="form-group">
            {{#each language}}
            <a id="foreign_language" href="/learningpage" for="language-1" style="color: white;">{{this.name}}</a>
            <img src="" alt="">
            <label for="language-1">native form</label>
                            <div class="login-glass card-container m-2">

                    <h3 class="m-2">
                        Lang 1 Title
                        <!-- {{language.id}}.
                        {{language.name}} -->
                    </h3>
                    <div class="image-container">

                    </div>
                    <h3 class="m-2">
                        Lang 1 Word
                        <!-- {{language.id}}.
                        {{language.name}} -->
                    </h3>
                </div>
            {{/each}}
        </div>


    </div>
</div>

<script src="/js/logout.js"></script>
{{!-- <script src="/js/select.js"></script> --}}